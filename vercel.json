{
  "version": 2,
  "builds": [
    {
      "src": "docu/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "docu/build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
